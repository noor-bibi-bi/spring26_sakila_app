name: Deployment Pipeline

on:
  push:
    branches:
      - main
    paths:
      - app.py
      - config.py
      - templates/**
      - static/**
      - Dockerfile
      - requirements.txt
      - docker-compose.yml

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment workflow triggered"